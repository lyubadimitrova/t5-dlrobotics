Change the name of the file "learn_weights.py" into "learn_weights_cma.py"

Add the codes between #TODO:... and #TODO into the files "learn_weights_cma.py" and "hole_reacher.py"

Save the model data into folder "cma_data"

Can directly load the model through the file "load_model_cma.py"

    
